<div class="row mb-3">
  <div class="col-lg-8 offset-lg-2">
    <%= form_with model: [@board, @comment], id: 'new_comment' do |f| %>
      <%= f.label :body, Comment.model_name.human %>
      <%= f.text_area :body, rows: 4, placeholder: Comment.model_name.human, id: 'js-new-comment-body', class: 'form-control mb-3' %>
      <%= f.submit (t 'defaults.post'), class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>